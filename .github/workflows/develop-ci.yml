name: 🔄 CI/CD Pipeline

on:
  push:
    branches: [develop]
  pull_request:
    branches: [main, develop]
    types: [opened, synchronize, reopened]

# Add permissions for security scanning
permissions:
  contents: read
  security-events: write
  actions: read
  pull-requests: read

env:
  DEVELOPER_DIR: /Applications/Xcode.app/Contents/Developer
  XCODE_PROJECT: FinanceApp.xcworkspace
  SCHEME: FinanceApp

jobs:
  # Alternative commitlint approach using Node.js
  commitlint:
    name: 📝 Validate Commit Messages
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "18"

      - name: Install commitlint
        run: |
          npm install --save-dev @commitlint/config-conventional @commitlint/cli

      - name: Validate commit messages
        run: |
          echo "📝 Validating commit messages..."

          # Get the base and head commits
          BASE_SHA="${{ github.event.pull_request.base.sha }}"
          HEAD_SHA="${{ github.event.pull_request.head.sha }}"

          echo "Base SHA: $BASE_SHA"
          echo "Head SHA: $HEAD_SHA"

          # Get commit messages in the PR
          git log --format="%H %s" $BASE_SHA..$HEAD_SHA > commits.txt

          # Validate each commit message
          EXIT_CODE=0
          while IFS= read -r line; do
            if [ -n "$line" ]; then
              COMMIT_SHA=$(echo "$line" | cut -d' ' -f1)
              COMMIT_MSG=$(echo "$line" | cut -d' ' -f2-)
              echo "Checking commit: $COMMIT_SHA"
              echo "Message: $COMMIT_MSG"
              
              # Use commitlint to check the message
              if ! echo "$COMMIT_MSG" | npx commitlint --verbose; then
                echo "❌ Commit message validation failed for: $COMMIT_SHA"
                EXIT_CODE=1
              else
                echo "✅ Commit message is valid for: $COMMIT_SHA"
              fi
            fi
          done < commits.txt

          if [ $EXIT_CODE -eq 0 ]; then
            echo "🎉 All commit messages are valid!"
          else
            echo "❌ Some commit messages are invalid. Please follow conventional commit format."
            echo "📖 Learn more: https://github.com/conventional-changelog/commitlint/#what-is-commitlint"
            exit 1
          fi

  test:
    name: 🧪 Run Tests & Lint
    runs-on: macos-15

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Select Xcode version
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "16.2.0"

      - name: Cache CocoaPods
        uses: actions/cache@v4
        with:
          path: Pods
          key: ${{ runner.os }}-pods-${{ hashFiles('**/Podfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-pods-

      - name: Install dependencies
        run: |
          gem install cocoapods
          pod install --repo-update

      - name: Install SwiftLint
        run: |
          brew install swiftlint

      - name: Run SwiftLint
        run: swiftlint lint --reporter github-actions-logging

      - name: Build for testing
        run: |
          echo "🔨 Starting build for testing..."
          echo "Workspace: ${{ env.XCODE_PROJECT }}"
          echo "Scheme: ${{ env.SCHEME }}"
          echo "Destination: iPhone 16"

          # First, let's see what's available
          xcodebuild -list -workspace ${{ env.XCODE_PROJECT }}

          # Check if the scheme exists and is valid
          xcodebuild -workspace ${{ env.XCODE_PROJECT }} \
                     -scheme ${{ env.SCHEME }} \
                     -showBuildSettings | head -20

          # Now attempt the build with verbose output
          set -o pipefail
          xcodebuild -workspace ${{ env.XCODE_PROJECT }} \
                     -scheme ${{ env.SCHEME }} \
                     -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest' \
                     clean build-for-testing \
                     -verbose | xcpretty --report html --output build-report.html
        continue-on-error: true

      - name: Run unit tests
        run: |
          echo "🧪 Running tests with full build cycle..."
          xcodebuild -workspace ${{ env.XCODE_PROJECT }} \
                     -scheme ${{ env.SCHEME }} \
                     -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest' \
                     test \
                     -resultBundlePath TestResults.xcresult

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results-${{ github.event_name }}-${{ github.run_number }}
          path: TestResults.xcresult

      - name: Upload build logs if failed
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: build-logs-${{ github.event_name }}-${{ github.run_number }}
          path: |
            build-report.html
            ~/Library/Developer/Xcode/DerivedData/*/Logs/Build/*.xcactivitylog

  security-scan:
    name: 🔒 Security Scan
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: "fs"
          scan-ref: "."
          format: "sarif"
          output: "trivy-results.sarif"

      - name: Upload Trivy scan results
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: "trivy-results.sarif"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        continue-on-error: true # Don't fail the workflow if code scanning is not enabled

      - name: Upload security scan artifact (fallback)
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: security-scan-results-${{ github.run_number }}
          path: trivy-results.sarif

      - name: Display security scan summary
        if: always()
        run: |
          echo "🔒 Security scan completed!"
          echo "📄 SARIF results saved as artifact: security-scan-results-${{ github.run_number }}"
          if [ -f "trivy-results.sarif" ]; then
            echo "📊 Scan file size: $(du -h trivy-results.sarif | cut -f1)"
            echo "💡 To enable automatic security alerts:"
            echo "   1. Go to repository Settings"
            echo "   2. Navigate to 'Code security and analysis'"
            echo "   3. Enable 'Code scanning' under 'GitHub Advanced Security'"
          fi

  semantic-version-develop:
    name: 📦 Development Versioning
    runs-on: ubuntu-latest
    needs: [test, commitlint]
    # Run on pushes to develop OR when manually triggered
    if: (github.ref == 'refs/heads/develop' && github.event_name == 'push') || github.event_name == 'workflow_dispatch'
    outputs:
      new_release_published: ${{ steps.semantic.outputs.new_release_published }}
      new_release_version: ${{ steps.semantic.outputs.new_release_version }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Semantic Release (Development)
        id: semantic
        uses: cycjimmy/semantic-release-action@v4
        with:
          semantic_version: 22
          extra_plugins: |
            @semantic-release/changelog@6.0.0
            @semantic-release/git@10.0.0
          dry_run: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Development Release Summary
        if: steps.semantic.outputs.new_release_published == 'true'
        run: |
          echo "🎉 New development release: v${{ steps.semantic.outputs.new_release_version }}"
          echo "📝 This version is ready for development and testing"
          echo "🚀 When ready for production, create a PR to main branch"

      - name: Update package.json version
        if: steps.semantic.outputs.new_release_published == 'true'
        run: |
          NEW_VERSION="${{ steps.semantic.outputs.new_release_version }}"
          echo "📦 Version updated to: $NEW_VERSION"
          echo "This version can be used for local development and testing"

  build-verification:
    name: 🏗️ Build Verification
    runs-on: macos-14
    needs: [test, semantic-version-develop]
    # Run if semantic versioning job completed (regardless of whether new release was published)
    # or if we're on develop branch and tests passed
    if: always() && needs.test.result == 'success' && (needs.semantic-version-develop.result == 'success' || (github.ref == 'refs/heads/develop' && github.event_name == 'push'))

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: develop

      - name: Select Xcode version
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: "16.2.0"

      - name: Cache CocoaPods
        uses: actions/cache@v4
        with:
          path: Pods
          key: ${{ runner.os }}-pods-${{ hashFiles('**/Podfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-pods-

      - name: Install dependencies
        run: |
          gem install cocoapods
          pod install --repo-update

      - name: Verify release build (Simulator only)
        run: |
          echo "🔨 Building release version for verification..."

          if [[ "${{ needs.semantic-version-develop.outputs.new_release_published }}" == "true" ]]; then
            echo "📦 Building version ${{ needs.semantic-version-develop.outputs.new_release_version }}"
            VERSION_INFO="Version ${{ needs.semantic-version-develop.outputs.new_release_version }}"
          else
            echo "📦 Building current develop branch (no new release)"
            VERSION_INFO="Current develop build"
          fi

          xcodebuild -workspace ${{ env.XCODE_PROJECT }} \
                     -scheme ${{ env.SCHEME }} \
                     -configuration Release \
                     -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest' \
                     clean build
                     
          echo "✅ Release build verification successful!"
          echo "📱 $VERSION_INFO is ready"
          echo "💡 This build is simulator-only (no device/archive needed for development)"
